// pages/api/luca.js (Corrected Import Path)
// **CORRECTED IMPORT** - Pointing up two levels to the root lib folder
import { LUCACore } from '../../lib/lucacore';

// ... (Rest of the code remains the same) ...

export default async function handler(req, res) {
    // ...
}